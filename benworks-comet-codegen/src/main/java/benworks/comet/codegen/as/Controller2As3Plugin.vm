#set( $class = $metadata )

// ${dontedit}
// Generated By Gecko,if you need to edit it,extends it! 

 package ${plugin.getPackage($class)}
{

#if ($class.comment)
  /** ${class.comment} */
#end
  import ${plugin.getPackage($class).GeneralRequest;
  public class ${plugin.getClassName($class)}
  {

#foreach( $jm in $class.getMethods() )
#if (${plugin.isSuitableMethod($jm)})
#set( $parameters = ${plugin.getParameters($jm)} )
#set( $responses = ${plugin.getResponses($jm)} )
   /** 
    * ${jm.comment} 
#foreach( $parameter in $parameters )
    *  @parm ${plugin.getParameterComment($parameter)}
#end
#foreach( $response in $responses )
    *  ${plugin.getResponseComment($response)}
#end
    */
    public static function ${jm.name}Request(${plugin.getParameterString($parameters,true)}):TdRequest{
    	var request:GeneralRequest = new GeneralRequest();  
    	request.action="${plugin.getAction($jm)}";
#foreach( $parameter in $parameters )
    	request.addParameter("${parameter.key}",${parameter.key});  
#end
    	return request;
    }
#end
#end
  }

}
